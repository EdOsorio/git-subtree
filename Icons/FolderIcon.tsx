import { folderContracted } from '@purplelab/healthnexus-library-ui';

const FolderIcon = () => {
  return <span dangerouslySetInnerHTML={{ __html: folderContracted }} />;
};
export default FolderIcon;
